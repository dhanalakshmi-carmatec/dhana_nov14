<tr>
    <td><%= contact.name %></td>
    <td class="actions">
      <%= link_to 'Show', user_contact_path(@user, contact)%>
      <%= link_to 'Edit', edit_user_contact_path(@user, contact)%>
      <%= link_to 'Delete', user_contact_path(@user, contact), method: :delete, data: {confirm: 'Are you sure'}%>
    
    </td>
</tr>