<hr class="border">
<%= form_for ([@user, @contact]) do |f| %>
  <%= @contact.errors.full_messages.to_sentence%>
    
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>  

    <div class="form-group">
      <%= f.submit %>
    </div>  
<% end %>
